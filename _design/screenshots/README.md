# Screenshots

视觉参考截图（基准设计快照）。

## `01-overview.png` (749×533)

主推方向 **A** 的完整截图，1440×1024 桌面画板的等比缩放（约 52%）。

> ⚠️ 这只是用于"一眼看清布局"的低分辨率参考。**所有精确尺寸、颜色、字体、间距请以同包的 `variant-a.jsx` + `styles.css` 源代码为准** — 用浏览器打开 `ScarletMu Home.html` 可以看到原生 1440×1024 全高保真渲染。

## 推荐使用方式

1. 把 `01-overview.png` 钉在编辑器旁边作为视觉锚点；
2. 实际开发时打开 `ScarletMu Home.html` 在 1440+px 显示器上参考，配合浏览器 DevTools 检查任何元素的 computed style。
